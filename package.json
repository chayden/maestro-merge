{
  "name": "meet-maestro-merge-helper",
  "version": "0.6.0",
  "private": true,
  "description": "Chrome extension for merging and organizing Meet Maestro heats.",
  "scripts": {
    "check": "node --check background.js && node --check content.js && node --check lib/api.js && node --check swimtopia-content.js",
    "test": "node --test",
    "package": "node scripts/package-extension.js"
  }
}
